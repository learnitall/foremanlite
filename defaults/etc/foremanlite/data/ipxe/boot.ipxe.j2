#!ipxe
cpuid --ext 29 && set arch x86_64 || set arch i386
iseq ${buildarch} arm64 && set arch arm64 ||

chain --autofree {{ url_for(endpoint, filename=startfile, _external=True) }}?arch=${arch}&mac=${mac:hexhyp}
