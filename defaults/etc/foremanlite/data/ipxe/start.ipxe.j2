#!ipxe
{% if provision %}
chain --autofree {{ url_for("ipxefiles", filename=provisionfile, _external=True) }}?arch=${arch}&mac=${mac:hexhyp}
{% else %}
chain --autofree {{ url_for("ipxefiles", filename=passfile, _external=True) }}?arch=${arch}&mac=${mac:hexhyp}
{% endif %}
